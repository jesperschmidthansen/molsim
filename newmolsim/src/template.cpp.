#include <octave/oct.h>
#include <omp.h>
#include "ms_misc.h"

#define HELPTXT ("Usage: XXX\n\nXXX\n")

DEFUN_DLD(FILENAME, args, ,HELPTXT){
	octave_value_list retval;

	if ( args.length() != XXX ){
		error(HELPTXT);
		return retval;
	}

	retval.append(XXX);
	return retval;
}
